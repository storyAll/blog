export const slideLeft = state => state.slideLeft

export const id = state => state.id

export const user = state => state.user

export const username = state => state.username

export const token = state => state.token
